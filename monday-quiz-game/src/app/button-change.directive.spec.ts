import { ButtonChangeDirective } from './button-change.directive';

describe('ButtonChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonChangeDirective();
    expect(directive).toBeTruthy();
  });
});
